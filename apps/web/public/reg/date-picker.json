{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "date-picker",
  "title": "DatePicker",
  "description": "A StyleX Date Picker component.",
  "dependencies": [
    "@stylexjs/stylex",
    "@phosphor-icons/react",
    "@base-ui/react"
  ],
  "registryDependencies": [
    "http://localhost:3001/reg/button.json",
    "http://localhost:3001/reg/calendar.json",
    "http://localhost:3001/reg/field.json",
    "http://localhost:3001/reg/popover.json",
    "http://localhost:3001/reg/theme.json"
  ],
  "files": [
    {
      "path": "packages/ui/src/components/DatePicker/date-picker.tsx",
      "content": "\"use client\";\n\nimport { CalendarBlankIcon } from \"@phosphor-icons/react\";\nimport { format } from \"date-fns\";\nimport { useRef, useState } from \"react\";\nimport { Button } from \"../Button/button\";\nimport { Calendar } from \"../Calendar/calendar\";\nimport { Field, FieldError, FieldLabel } from \"../Field/field\";\nimport {\n  Popover,\n  PopoverCompound,\n  PopoverPopup,\n  PopoverPositioner,\n  PopoverTrigger,\n} from \"../Popover/popover\";\n\ntype Props = {\n  value: Date | undefined;\n  onChange: (date: Date | undefined) => void;\n  label?: string;\n  placeholder?: string;\n  error?: string;\n  disabled?: boolean;\n};\n\nexport function DatePicker({\n  value,\n  onChange,\n  label,\n  placeholder = \"Pick a date\",\n  error,\n  disabled,\n}: Props) {\n  const [open, setOpen] = useState(false);\n  const triggerRef = useRef<HTMLButtonElement>(null);\n\n  const handleSelect = (date: Date | undefined) => {\n    onChange(date);\n    setOpen(false);\n  };\n\n  return (\n    <Field>\n      <Popover open={open} onOpenChange={setOpen}>\n        {label && (\n          <FieldLabel\n            onClick={() => {\n              triggerRef.current?.click();\n              triggerRef.current?.focus();\n            }}\n          >\n            {label}\n          </FieldLabel>\n        )}\n        <PopoverTrigger\n          ref={triggerRef}\n          disabled={disabled}\n          render={<Button type=\"button\" variant=\"outline\" fullWidth />}\n        >\n          <CalendarBlankIcon size={18} />\n          {value ? format(value, \"PPP\") : placeholder}\n        </PopoverTrigger>\n        <PopoverCompound.Portal>\n          <PopoverPositioner sideOffset={6} side=\"bottom\" align=\"start\">\n            <PopoverPopup>\n              <Calendar mode=\"single\" selected={value} onSelect={handleSelect} />\n            </PopoverPopup>\n          </PopoverPositioner>\n        </PopoverCompound.Portal>\n      </Popover>\n      {error && <FieldError>{error}</FieldError>}\n    </Field>\n  );\n}\n",
      "type": "registry:ui",
      "target": "app/components/ui/DatePicker/date-picker.tsx"
    }
  ],
  "type": "registry:ui"
}