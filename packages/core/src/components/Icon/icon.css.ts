import { baseSprinkles } from "../../utils/sprinkles";

export const iconWrapper = baseSprinkles({
  display: "inline-flex",
  align: "center",
  justify: "center",
  width: "fit",
  height: "fit",
  shrink: 0,
});
